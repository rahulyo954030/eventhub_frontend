# Backend URL for API proxy (Vercel + local dev). No /api suffix.
BACKEND_URL=http://localhost:5000

# Optional legacy alias — BACKEND_URL is preferred on Vercel
NEXT_PUBLIC_API_URL=http://localhost:5000/api
